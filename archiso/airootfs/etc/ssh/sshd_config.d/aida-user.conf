# Drop the 'aida' system user straight into aida-chat when they SSH in.
# Regular users get a normal shell.
Match User aida
    ForceCommand /opt/aida-os/.venv/bin/aida-chat
    AllowTcpForwarding no
    X11Forwarding no
