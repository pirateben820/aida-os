# Development infrastructure only — NATS message bus
# Run: docker compose -f docker-compose.dev.yml up -d

services:
  nats:
    image: nats:2-alpine
    ports:
      - "4222:4222"   # client
      - "8222:8222"   # HTTP monitoring
    command: ["--http_port", "8222"]
    restart: unless-stopped
